services:
  - type: web
    name: system-board-audit-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ULTRALYTICS_NO_CHECK
        value: "1"
    plan: free
